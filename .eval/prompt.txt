The Neo4j Cypher language-support artifacts (semantic analyzer and grammar) were just updated to a new version. As a result the build, tests, lint, and/or formatting checks are now failing. Investigate the failures and make the minimum set of changes needed to get the repo green again. Verify by running pnpm build, pnpm test, pnpm lint, and pnpm format:check. Do not skip, disable, or delete tests to make them pass.

The artifact bump touched two large generated files that you should NOT read in full or diff: packages/language-support/src/syntaxValidation/semanticAnalysis.js (~13k lines, TeaVM-transpiled) and packages/vscode-extension/syntaxes/cypher.json (TextMate grammar). Treat them as opaque. If you need to understand what changed, infer from the test failures and the human-readable grammar files in packages/language-support/src/antlr-grammar/ — not from the generated JS or JSON.
