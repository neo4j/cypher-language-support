{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["out/**"],
      "dependsOn": ["^build"]
    },
    "clean": {},
    "watch": {},
    "test": {}
  }
}
