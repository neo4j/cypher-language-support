# Simple match test


## Cypher blocks

```cypher
MATCH (n:Label) RETURN function(n.property)
```

## Js/Ts blocks

```ts
const a = "/*cypher*/ MATCH (n:Label) RETURN function(n.property)"
```

```js
const a = "/*cypher*/ MATCH (n:Label) RETURN function(n.property)"
```

## Java blocks

```java
String a = "/*cypher*/ MATCH (n:Label) RETURN function(n.property)"
```

## Python blocks

```python
a = "/*cypher*/ MATCH (n:Label) RETURN function(n.property)"
```

## Golang blocks

```go
var a = "/*cypher*/ MATCH (n:Label) RETURN function(n.property)"
```

## .NET blocks

```cs
var a = "/*cypher*/ MATCH (n:Label) RETURN function(n.property)"
```

```fsharp
let a = "/*cypher*/ MATCH (n:Label) RETURN function(n.property)"
```

